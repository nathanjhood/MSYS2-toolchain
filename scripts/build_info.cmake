set(BUILD_INFO_FILE_PATH ${CURRENT_PACKAGES_DIR}/BUILD_INFO)
file(WRITE  ${BUILD_INFO_FILE_PATH} "CRTLinkage: ${CRT_LINKAGE}\n")
file(APPEND ${BUILD_INFO_FILE_PATH} "LibraryLinkage: ${LIBRARY_LINKAGE}\n")

if (DEFINED MSYS_POLICY_DLLS_WITHOUT_LIBS)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyDLLsWithoutLIBs: ${MSYS_POLICY_DLLS_WITHOUT_LIBS}\n")
endif()
if (DEFINED MSYS_POLICY_DLLS_WITHOUT_EXPORTS)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyDLLsWithoutExports: ${MSYS_POLICY_DLLS_WITHOUT_EXPORTS}\n")
endif()
if (DEFINED MSYS_POLICY_DLLS_IN_STATIC_LIBRARY)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyDLLsInStaticLibrary: ${MSYS_POLICY_DLLS_IN_STATIC_LIBRARY}\n")
endif()
if (DEFINED MSYS_POLICY_MISMATCHED_NUMBER_OF_BINARIES)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyMismatchedNumberOfBinaries: ${MSYS_POLICY_MISMATCHED_NUMBER_OF_BINARIES}\n")
endif()
if (DEFINED MSYS_POLICY_EMPTY_PACKAGE)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyEmptyPackage: ${MSYS_POLICY_EMPTY_PACKAGE}\n")
endif()
if (DEFINED MSYS_POLICY_ONLY_RELEASE_CRT)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyOnlyReleaseCRT: ${MSYS_POLICY_ONLY_RELEASE_CRT}\n")
endif()
if (DEFINED MSYS_POLICY_ALLOW_OBSOLETE_MSVCRT)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyAllowObsoleteMsvcrt: ${MSYS_POLICY_ALLOW_OBSOLETE_MSVCRT}\n")
endif()
if (DEFINED MSYS_POLICY_EMPTY_INCLUDE_FOLDER)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyEmptyIncludeFolder: ${MSYS_POLICY_EMPTY_INCLUDE_FOLDER}\n")
endif()
if (DEFINED MSYS_POLICY_ALLOW_RESTRICTED_HEADERS)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyAllowRestrictedHeaders: ${MSYS_POLICY_ALLOW_RESTRICTED_HEADERS}\n")
endif()
if (DEFINED MSYS_POLICY_SKIP_DUMPBIN_CHECKS)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicySkipDumpbinChecks: ${MSYS_POLICY_SKIP_DUMPBIN_CHECKS}\n")
endif()
if (DEFINED MSYS_POLICY_SKIP_ARCHITECTURE_CHECK)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicySkipArchitectureCheck: ${MSYS_POLICY_SKIP_ARCHITECTURE_CHECK}\n")
endif()
if (DEFINED MSYS_POLICY_CMAKE_HELPER_PORT)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicyCmakeHelperPort: ${MSYS_POLICY_CMAKE_HELPER_PORT}\n")
endif()
if (DEFINED MSYS_POLICY_SKIP_ABSOLUTE_PATHS_CHECK)
    file(APPEND ${BUILD_INFO_FILE_PATH} "PolicySkipAbsolutePathsCheck: ${MSYS_POLICY_SKIP_ABSOLUTE_PATHS_CHECK}\n")
endif()
if (DEFINED MSYS_HEAD_VERSION)
    file(APPEND ${BUILD_INFO_FILE_PATH} "Version: ${MSYS_HEAD_VERSION}\n")
endif()
